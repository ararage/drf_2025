server {
    client_max_body_size 50M;
    listen 80 default_server;
    server_name _;

    location / {
        proxy_pass http://django_develop:5000;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }
}